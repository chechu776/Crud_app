<%- include("partials/head.ejs") %>
    <%- include("partials/nav.ejs") %>
        <main class="mx-auto flex h-10/12 w-full items-center justify-center absolute top-20 bg-gray-700 text-white">
            <section class="flex w-[30rem] flex-col space-y-10">
                <div class="text-center text-4xl font-medium">Edit User (<%= user.name %>)</div>
                <form action="/update/<%= user._id %>" method="post" id="add-form" enctype="multipart/form-data"  class="flex w-[30rem] flex-col space-y-10">
                    <div
                        class="w-full transform border-b-2 bg-transparent text-lg duration-300 focus-within:border-indigo-500">
                        <input type="text" placeholder="Name" name="name"required value="<%= user.name %>"
                            class="w-full border-none bg-transparent outline-none placeholder:italic focus:outline-none" />
                    </div>

                    <div
                        class="w-full transform border-b-2 bg-transparent text-lg duration-300 focus-within:border-indigo-500">
                        <input type="text" placeholder="Phone number" name="phone" required value="<%= user.phone %>"
                            class="w-full border-none bg-transparent outline-none placeholder:italic focus:outline-none" />
                    </div>

                    <div
                        class="w-full transform border-b-2 bg-transparent text-lg duration-300 focus-within:border-indigo-500">
                        <input type="text" placeholder="Email" name="email"required value="<%= user.email %>"
                            class="w-full border-none bg-transparent outline-none placeholder:italic focus:outline-none" />
                    </div>

                    <div
                        class="w-full transform border-b-2 bg-transparent text-lg duration-300 focus-within:border-indigo-500">
                        <input type="text" placeholder="Password" name="password" required value="<%= user.password %>"
                            class="w-full border-none bg-transparent outline-none placeholder:italic focus:outline-none" />
                    </div>
                    <div class="flex gap-8 ">
                        <label for="" class="">Upload image</label>
                        <input type="file" class="cursor-pointer" name="image" >
                        <img src="/<%= user.image %>" alt="" class="w-30 h-30">
                    </div>
                    <input type="hidden" name="old_image" value="<%= user.image %>">
                    <button
                        class="transform rounded-sm bg-indigo-600 py-2 font-bold duration-300 hover:bg-indigo-400">Update</button>
                </form>

            </section>
        </main>

        <%- include("partials/footer.ejs") %>